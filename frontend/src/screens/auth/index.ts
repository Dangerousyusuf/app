export { default as LoginScreen } from './LoginScreen';
export { default as RegisterScreen } from './RegisterScreen';

// Export auth screen types
export type { LoginScreenProps, RegisterScreenProps } from '../../types';