# Frontend Environment Configuration
# Copy this file to .env and update values for your environment

# =============================================================================
# PRODUCTION CONFIGURATION (HTTPS)
# =============================================================================

# Production API URLs (HTTPS enforced)
EXPO_PUBLIC_API_BASE_URL=https://api.yourdomain.com/api/v1
EXPO_PUBLIC_BACKEND_URL=https://backend.yourdomain.com/api

# Production Domain
EXPO_PUBLIC_DOMAIN=yourdomain.com

# =============================================================================
# DEVELOPMENT CONFIGURATION (HTTP)
# =============================================================================

# Development API URLs (Auto-detected from Metro bundler)
# Leave empty to use auto-detection based on Metro bundler IP
# EXPO_PUBLIC_DEV_API_BASE_URL=http://192.168.1.100:3001/api/v1
# EXPO_PUBLIC_DEV_BACKEND_URL=http://192.168.1.100:3000/api

# =============================================================================
# APP CONFIGURATION
# =============================================================================

# App Environment
EXPO_PUBLIC_NODE_ENV=production

# App Version
EXPO_PUBLIC_APP_VERSION=1.0.0

# Feature Flags
EXPO_PUBLIC_ENABLE_ANALYTICS=true
EXPO_PUBLIC_ENABLE_CRASH_REPORTING=true
EXPO_PUBLIC_ENABLE_DEBUG_MODE=false

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================

# Token Storage Strategy
# - production: SecureStore (mobile) + HttpOnly cookies (web)
# - development: AsyncStorage fallback
EXPO_PUBLIC_SECURE_STORAGE=true

# API Timeout (milliseconds)
EXPO_PUBLIC_API_TIMEOUT=10000

# =============================================================================
# PLATFORM SPECIFIC
# =============================================================================

# iOS Configuration
EXPO_PUBLIC_IOS_BUNDLE_ID=com.yourdomain.app

# Android Configuration
EXPO_PUBLIC_ANDROID_PACKAGE=com.yourdomain.app

# Web Configuration
EXPO_PUBLIC_WEB_TITLE=Your App Name

# =============================================================================
# MIGRATION NOTES
# =============================================================================
# 
# HTTPS Migration Checklist:
# 1. Update EXPO_PUBLIC_API_BASE_URL to your HTTPS API Gateway URL
# 2. Update EXPO_PUBLIC_BACKEND_URL to your HTTPS Backend URL
# 3. Set EXPO_PUBLIC_DOMAIN to your actual domain
# 4. Enable EXPO_PUBLIC_SECURE_STORAGE for production
# 5. Test all API calls with HTTPS URLs
# 6. Verify SecureStore functionality on mobile devices
# 
# Development vs Production:
# - Development: Uses auto-detected IP from Metro bundler
# - Production: Uses explicit HTTPS URLs from environment variables
# - Token storage: SecureStore (mobile) vs AsyncStorage (fallback)
# 
# Security Notes:
# - Never commit actual production URLs to version control
# - Use different domains for staging and production
# - Enable secure storage for production builds
# - Test HTTPS connectivity before deployment
# =============================================================================